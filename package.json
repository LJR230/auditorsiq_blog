{
  "name": "auditorsiq-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "blog.auditorsiq.com — Astro static site, deployed by Cloudflare Pages from main.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.4.1",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  },
  "engines": {
    "node": ">=22"
  }
}
